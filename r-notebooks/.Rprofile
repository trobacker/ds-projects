source("renv/activate.R")

# Set CRAN mirror
options(repos = c(CRAN = "https://cloud.r-project.org"))

# Optional: Print message when renv activates
cat("renv activated for Bayesian Decision Theory tutorials\n")
cat("Run 'renv::status()' to check environment status\n")
